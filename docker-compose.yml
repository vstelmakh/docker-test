version: "3.4"

services:
    nginx:
        build:
            context: .
            dockerfile: ./docker/Dockerfile
            target: nginx_test
        # image env variables defined in build workflow
        image: $IMAGE_NGINX:$VERSION
        ports:
            - 80:80
        environment:
            - TZ
